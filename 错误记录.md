# 错误记录（Music / Anglo 学习页）

按时间倒序追加。写清：**现象 → 原因 → 以后怎么避免**。

---

## 2026-07-26 · Belaya 三条小改被后台代理拖死

### 现象

- 用户只要 3 处简单修改（Belaya Koshka：`ле-` → 低八度 6；俄文标题不要「小白猫」；删多余 tip + 修歌词/节奏）。
- Parent 把需求叠进长时间跑的后台子代理，并反复 interrupt。
- Agent 口头「正在改」，约 1.5 小时磁盘上仍无改动。
- 沟通里一度把延误怪到用户「叠需求」——不对，是流程失败。

### 原因

1. **简单编辑交给可被打断的长后台 worker**，进度被 reset，而不是本轮直接改完。
2. **未用磁盘验收就声称进度**（没有 `grep`/读 canonical 确认确切字符串）。
3. **滴灌 interrupt** 代替「完整清单一次做完」或「做完再接下一批」。

### 以后怎么避免

| 做 | 不做 |
|----|------|
| 小改直接改 `index.html`（或一个短命 worker 带完整清单） | 把小改停在不断 interrupt 的后台代理上 |
| 上报前 `grep`/Read 确认落盘（如 `a'` 不是 `a''`） | 口头「正在改 / 已改完」而磁盘没有 |
| 多条相关小需求合并一次编辑；延误道歉修流程 | 怪用户「叠小需求」 |
| Multitask 时一个 worker + 完整清单；超时几分钟则停掉自己改 | 滴灌 interrupt 拖数小时 |

规则：`.cursor/rules/agent-delivery.mdc`（`alwaysApply: true`）。

---

## 2026-07-25 · 自由簧音色被「变回」三角波

### 现象

- 已把 `playAbcNote` 从 `osc.type = "triangle"` 改成 `createPeriodicWave` 自由簧合成。
- 过一会儿听起来又像蜂鸣器；磁盘上又变回 `triangle`。
- 同一次会话里修复后又丢过一次。

### 原因（叠加，不是单一 bug）

1. **编辑器缓冲区盖盘（主因）**  
   `index.html` 一直在 IDE 里打开。Agent 改的是磁盘；编辑器里若仍是旧内容，一旦自动保存 / 焦点保存 / 手动保存，就会用旧缓冲把磁盘上的新音色盖掉。

2. **多份「正文」互相 `cp`**  
   同时存在：
   - `index.html`
   - `anglo-intro.html`
   - `anglo-concertina/index.html`
   - `anglo-concertina/anglo-intro.html`  
   历史流程里常用 `cp A B`「同步」。只要源文件是旧的，一次拷贝就会把新改动（音色、按钮逻辑）整页冲掉，并复制到所有副本。

3. **并行 Agent 改同一套 HTML**  
   多个子任务同时改 layout / 标题 / 推送，读到的是过期快照再写回，造成丢失更新。

### 已做处理

- 恢复自由簧（`createPeriodicWave` / `reedWave`）。
- 删除 `anglo-concertina/*.html` 副本，该目录只留说明，避免再当正文编辑。
- `index.html` 为唯一源；需要时再同步到 `anglo-intro.html`。
- 增加 Cursor 规则：`.cursor/rules/html-single-source.mdc`。

### 以后禁止 / 必须

| 做 | 不做 |
|----|------|
| 只改根目录 `index.html`，确认后再 `cp` 到 `anglo-intro.html` | 在 `anglo-concertina/` 再放学习页 HTML |
| 改音频后 `grep createPeriodicWave index.html` 验收 | 盲 `cp` 而不先检查源是否含关键改动 |
| 发现「刚改又没了」先让用户从磁盘重新加载已打开文件 | 并行多个 Agent 写同一 HTML |
| 小范围 `StrReplace` / 脚本替换 | 用过期全文 `Write` 覆盖整页 |

### 验收标记

音频未丢时，`index.html` 中应存在：`reedWave`、`createPeriodicWave`；`playAbcNote` 内不应再有 `osc.type = "triangle"`。
