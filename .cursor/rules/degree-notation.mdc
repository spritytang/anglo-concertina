---
description: 简谱升降号与度数必须同一行
alwaysApply: true
---

# 简谱记谱：升降号同行

改 `index.html`（或任何渲染简谱的 UI）时：

- **升降号（♯ / ♭）必须与度数数字写在同一行**，例如 `♯5`、`♭7`，不要把 `#` / `b` 单独占一行。
- 八度点（`·`）可以在数字**上方**另起一行；升降号仍贴在数字左侧同一行。
- 实现约定：`formatDeg` 用 `.jp-deg` 包裹 `acc + num`；带高音点时结构为 `.oct`（上点）> `.jp-deg`（♯/♭ + 数字）。**禁止**把 `.acc` 放在 `.oct` 外面当兄弟节点（会在竖排/窄格里掉成三行：`#` / `·` / `5`）。
- ABC 模式：升降号与音名同一主行（已有 `.abc-main`）。

验收：音步条里 `g#''` 应显示为上一行 `·`、下一行 `♯5`，而不是三行 `♯` / `·` / `5`。
