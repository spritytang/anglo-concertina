---
description: Anglo 扒谱、左手和声、少换向指法质量标准
alwaysApply: true
---

# Anglo 编配质量（旋律 / 左手 / 推拉）

Wren 2 · C/G · Wheatstone。改 Fly Me、民谣谱、左手伴奏或跨行指法时必须遵守。

**Wren 2 右手角键（以实琴为准，勿抄错八度）：**
- 顶行最右拉 = **F6**（`f'''`），不是 F7
- 底行最右拉 = **♯F6**（`f#'''`），不是 ♯F7

## 1. 旋律（扒谱）

- **音高优先**：禁止为减少推拉换向而改音高或乱跳八度。
- **先锁定参照**：用户给了 Real Book / 简谱 / 视频时间戳时，以该参照为准；没有参照就先问，不要凭记忆硬编整曲。**禁止只凭歌名默写旋律。**
- **度数串验收**：写入 `sourceDegrees`（纯数字），再用 steps 反推度数串，二者必须完全一致后再改指法（详见 skill `anglo-arrange`）。
- **逐句验收**：用户点名某句（如「Jupiter and Mars」）时，只核对该句音名+八度，改完用简谱度数核对（例：标准收尾是 `5–3–2–1` = G E D C，不是 `5–3–2–2–1··`）。
- **八度一致**：同一乐句内保持与前后句同一音区；不要把收尾擅自升到 `c'''` 除非参照如此。
- **键位图左右**：屏幕左=左手、右=右手（持琴视角），不要因 `focusSide` 对调。

## 2. 左手和声

- **先要和弦进行**：至少 Verse/Chorus 的 lead sheet（如 `Am–Dm–G–C–F–Bm7b5–E7–Am`）。没有进行时先问，不要随便配 Dm/Am。
- **Anglo 约束**：同一拍左右手必须**同向**（推或拉）；用 `flymeChordForStep` 一类逻辑做同向替代（如拉 Am / 推 Em），不要推拉混按。
- **功能要对**：收束在大调主和弦的乐句不要配小三和弦（反例：Jupiter/Mars 应走 **C**，不该整句 Dm）。
- **默认密度**：可开关的「左手伴奏」用同向三和弦；用户要根音-only 时再简化。

## 3. 指法与推拉流畅

- **两遍流程**：① 音高+和弦正确 → ② 再压换向次数。冲突时保留①。
- **优先跨行同向备用**（Wheatstone RH）：顶②拉 G、底③拉高 C、底②推 B、顶②推 A、底①推 G；F（`f''`）右手通常只有拉；E（`e''`）右手通常只有推——拉句里的同八度 3 用**左手底⑤拉** `e''`（Fly Me 字母 `y`），不要为省换向改音高。
- **禁止空换向**：不要「推 G 再立刻拉同音 G」之类无意义掉头。
- **能量化时**：改完后数整曲/该段推拉换向次数；能不增音高错误则只减换向。

## 4. 协作短清单（缺一项就问）

```
参照：链接或简谱（旋律以谁为准）
调性 / 音域：如 C/Am，右手中行附近
和声：和弦进行 + 左手规则（同向三和弦 / 可替代）
指法：少换向目标；可用跨行键；禁止的八度
验收：先某段；错句用歌词定位
```

## 5. 实现落点

- 只改 canonical `index.html`，验证后同步 `anglo-intro.html`（见 `html-single-source` 规则）。
- Fly Me 字母表与 `FLYME_RH` / `flymePhrase` 数组长度必须一致后再交付。
- 简谱升降号与度数同一行（见 `jianpu-notation` 规则）；改 `formatJianpu` 时勿把 `.acc` 放到 `.oct` 外。
